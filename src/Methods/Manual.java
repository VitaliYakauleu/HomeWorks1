package Methods;

import java.util.Random;

public class Manual {
    public static void main(String[] args) {

        Random random = new Random();
        //------arr[i] = random.nextInt(163, 191-1);  запалняет массив случайными числами

        //-------Метод Arrays.toString() возвращает строковое представление одномерного массива,
        // разделяя элементы запятой. Вместо того чтобы перебирать массивы циклом for,
        // можно воспользоваться этим методом для вывода элементов на консоль:
        //int[] array = {1, 4, 6, 3, 8};
//        System.out.println(Arrays.toString(array));


        //---------// Метод для проверки наличия элемента в массиве
        //    public static boolean contains(int[] array, int number) {
        //        for (int i = 0; i < array.length; i++) {
        //            if (array[i] == number) {
        //                return true;
        //            }
        //        }
        //        return false;
        //    }

        // String sub2 = sourceWord.substring(3, 8); // "грамм"
        // метод substring вырезает из строки часть строки по заданному диапазану
        //String sourceWord = "программа";


        //------------------------------------------
        //String sentence = "если много мучаться - что нибудь получиться";
        //        char[] charArray = sentence.toCharArray();// конвертируем строку в массив символов


        //-----------------------------------------





    }


}
